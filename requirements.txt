curl_cffi>=0.7.0
openpyxl>=3.1.0
pdfminer.six>=20231228
streamlit>=1.32.0
supabase>=2.4.0
# AI analysis (Gemini)
google-generativeai>=0.7.0
# bot_listener_cloud.py only — but harmless on Streamlit Cloud
flask>=3.0.0
gunicorn>=21.2.0

# Optional — semantic CV scoring via sentence-transformers.
# Adds ~800MB (PyTorch + model). May exceed Streamlit Cloud's 1GB RAM limit.
# Without this, CV matching falls back to keyword-only scoring (still works).
# Uncomment for local install or paid hosting:
# sentence-transformers>=2.7.0
