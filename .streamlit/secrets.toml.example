# Copy this file to .streamlit/secrets.toml (gitignored) for LOCAL testing,
# OR paste these values into the "Secrets" tab on Streamlit Cloud's app dashboard.
#
# All keys are optional — if unset, the UI shows empty fields you can fill manually.

[telegram]
token = "8693259913:AAGa5K-LKLMF-jvBhOjFlX2W2vfwZ8OXca0"
chat_id = "1639658662"

[defaults]
# Pre-fill the form on first load. Users can override any field.
source = "cpjobs"
keyword = "Accountant"
location = "Hong Kong Island"
max_pages = 1
delay = 1.5
full_jd = true
match_threshold = 0
